.DS_Store
.venv/
__pycache__/
*.pyc
tmp/

# Local data — NEVER commit (large SQLite archives, 40GB+, and exports).
# These are regenerable from the .xlsx workbooks via scripts/export_workbook_to_sqlite.py
# and are operational/archival state, not source.
data/

# Generated product artifacts (the sellable PDF is built locally)
product/dist/

# Misc local scratch
played

# Private / non-public material — monetization, launch copy, internal ops,
# preserved originals. Kept locally, never published.
private/
