# Structural Exception Layer Search

Generated by `structural_exception_layer_search.py`.

Scope: mechanical code/table/rendering evidence only. No translation,
glossary, or plaintext is created or promoted.

## Question

Do the known anomalies form a compact structural layer over the existing
mechanical formula, or are they just isolated lookup patches?

## Incremental Models

Reference saturated ordered lookup: 376.9 bits for 99 ordered labels.

| Model | Correct | Raw acc | Residuals | Lossless MDL bits | Delta vs ordered | Delta vs previous |
|---|---:|---:|---:|---:|---:|---:|
| `unordered_lookup` | 98/99 | 0.990 | 1 | 219.9 | 157.1 | n/a |
| `unordered_lookup_plus_directed_render` | 99/99 | 1.000 | 0 | 223.9 | 153.1 | -4.0 |
| `directed_render_plus_exception_layer` | 99/99 | 1.000 | 0 | 238.5 | 138.4 | -14.6 |
| `diagonal_E_anchors_plus_exceptions` | 99/99 | 1.000 | 0 | 246.7 | 130.2 | -8.2 |
| `mirror_lower_plus_exceptions` | 99/99 | 1.000 | 0 | 238.5 | 138.4 | 8.2 |

Selected compact structural layer: `mirror_lower_plus_exceptions`.

The selected layer is not the cheapest possible lossless notation once an
unordered pair lookup is already allowed. Its value is structural: it
derives the lower surface as a mirror of the upper surface and isolates
the two remaining lower-side anomalies.

## Controls

| Test | Observed | Control mean | Control max | p | Interpretation |
|---|---:|---:|---:|---:|---|
| mirror lower matches | 43 | 4.98 | 14 | 0.00005 | strong structural signal |
| random exception picks hit 91+93 | 2 | 0.045 | 2 | 0.00045 | known exceptions are compact, but still exceptions |
| diagonal E pressure | 5 | 1.99 | 7 | 0.01745 | suggestive only |
| 33/66 exact diagonal anchor set | 2 | 0.40 | 2 | 0.02245 | conditional anchor is narrow |
| prior tape-only/literal-only Bonferroni | 0.03130 raw p | n/a | n/a | 0.18779 | not promoted after multiplicity |
| zero geometry holdout MDL | 11.6 bits | n/a | n/a | 0.00300 | separate zero-render support |

## Anomaly Accounting

| Anomaly | Status in this layer | What is derived | What remains lookup/metadata |
|---|---|---|---|
| `missing_ordered_39_lower_orphan_93` | compact metadata in mirror model | upper 39 absence explains why mirror rendering cannot cover lower 93 | 93=N is still stored as orphan metadata; N is not derived |
| `conflict_19_91` | explicit exception | the layer isolates it as the only reverse-available lower conflict | 91=N is not predicted by the mirror rule; it is an explicit directed exception |
| `tape_only_diagonal_E_33_66` | weak anchor, not selected | a possible rule can mark pair-only-outside diagonal cells as E | prior Bonferroni control remains non-promoted; E is still an anchor label |
| `diagonal_E_pressure` | suggestive pressure only | diagonal E count is measured against inventory-preserving shuffles | does not recover the full diagonal or table placement |
| `zero_omission_geometry` | separate supporting render layer | previous-code descending/diagonal geometry improves zero-omission holdout MDL | zero rendering is not a symbol-label generator and remains incomplete |
| `mirror_lower` | strong structural layer | 43/44 reverse-available lower pairs copy the upper symbol | the upper/unordered labels themselves remain lookup |

## Verdict

The anomalies do form a compact render/exception layer for the ordered surface: mirror lower is far above shuffled controls, and the only residual lower-side codes are 91 and 93. The layer saves substantial MDL versus a saturated ordered lookup, but it does not beat the most compact unordered-pair lookup with one residual correction. Diagonal E anchors and zero-omission geometry are useful mechanical side evidence, not a promoted table-origin formula.

Classification: `compact_render_layer_over_lookup_not_new_matrix_formula`.
Translation delta: `NONE`.
