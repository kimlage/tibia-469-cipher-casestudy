# Final Physical Topology Report

Verdict: `partial_topology_integrated_no_promotion`. Translation delta: `NONE`.

The 2026-06-20 physical-topology research was incorporated as a bounded
mechanical evidence front. It changes the project record by adding a partial
public topology seed and explicit H-TOP hypotheses; it does not change the
closed semantic verdict.

## Accepted

- Hellgate has a public bookcase/overview-order source suitable for a partial
  manifest.
- Isle shelf 21 and shelf 39 are strong community-wiki anchors.
- Kharos/Ferumbras is a watchlist/weak holdout until exact text or independent
  indexing is available.
- Topology should ask whether physical placement predicts modules/copies, not
  whether position translates words.

## Not Accepted

- No exact tile/slot/orientation/read-order manifest is public and committed.
- Public overview order is not accepted as authorial order.
- The TibiaSecrets mathematical/topological claims are not promoted without
  local tests and controls.
- No topology-derived plaintext or symbol mapping is promoted.

## Tests

The two generated audits are:

- `01_public_topology_manifest_audit`: creates the partial seed-to-local
  mapping and records ambiguity. Current result: 71 public seed entries, 70
  local DB books, 65 resolved unique entries, 6 ambiguous entries, 0 unmatched.
- `02_topology_mechanical_signal_audit`: tests public order and bookcase
  grouping against deterministic shuffles over row0 similarity. Current result:
  public-order `p(control >= observed) = 0.1460`; bookcase-group
  `p(control >= observed) = 0.7215`; classification
  `no_promoted_topology_signal`.

The Isle shelf 39 prefix `6512889672` maps cleanly to a Hellgate/local 469
candidate, but under the Fandom overview seed it is Hellgate public entry `31`,
not `35`. That makes the TibiaSecrets `Book 35` claim an indexing mismatch to
audit against its image numbering, not an accepted rejection.
