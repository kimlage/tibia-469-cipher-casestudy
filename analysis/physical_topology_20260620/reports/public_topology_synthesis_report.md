# Public Topology Synthesis Report

Verdict: `partial_public_topology_ready_fine_topology_blocked`.
Translation delta: `NONE`.

The public evidence supports macro and bookcase-level topology:

| Area | Status | Use |
|---|---|---|
| Hellgate Library | 70-book corpus and overview/bookcase order are public | partial topology seed |
| Isle shelf 21 | *Beware of the Bonelords* | metatext/context anchor |
| Isle shelf 39 | `6512889672 (Book)` | external 469 holdout |
| Kharos/Ferumbras | community visual/article evidence | watchlist only |

The missing layer remains:

```text
book_id -> exact tile -> shelf side -> slot -> orientation -> map version
```

That missing layer blocks any promoted route/order/graph hypothesis. Public
bookcase topology can still be used to test whether location predicts modules,
copies, or row0 mechanical similarity better than shuffles.
